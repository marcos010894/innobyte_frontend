# 🔧 Configuração de Ambiente

Este arquivo contém as variáveis de ambiente para **PRODUÇÃO** no Fly.io.

## 📝 Variáveis Disponíveis

### `VITE_API_URL` (obrigatório)
URL do backend da API.

**Exemplo**:
```env
VITE_API_URL=https://etiquetas-sys-backend.fly.dev/api
```

**⚠️ IMPORTANTE**:
- Sempre terminar com `/api`
- Usar HTTPS em produção
- Não usar `localhost` aqui (apenas em `.env.local`)

### `NODE_ENV` (automático)
Define o ambiente de execução.

```env
NODE_ENV=production
```

## 🚀 Como Usar

1. **Build local** (usa automaticamente):
```bash
npm run build
```

2. **Deploy Fly.io** (usa automaticamente):
```bash
flyctl deploy
```

3. **Sobrescrever via Fly.io Secrets** (opcional):
```bash
flyctl secrets set VITE_API_URL=https://nova-url.fly.dev/api
```

## 🔐 Segurança

- ✅ Este arquivo **pode** ser commitado (não contém secrets)
- ✅ URLs públicas são OK aqui
- ❌ **NUNCA** adicione tokens, senhas ou API keys aqui
- ❌ Para dados sensíveis, use `flyctl secrets set`

## 📚 Mais Informações

- [Vite Env Variables](https://vitejs.dev/guide/env-and-mode.html)
- [Fly.io Secrets](https://fly.io/docs/reference/secrets/)
